<!-- <% if (typeof error === 'string') { %>
    <%
        swal(<%= error %>, "error")
    %>
<% } %>
    
<% if (typeof success === 'string') { %>
    <%
        swal(<%= success %>, "success")
    %>
<% } %> -->

<% if (typeof success === 'string') swal(success, "success") %>